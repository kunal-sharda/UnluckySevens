# Unlucky Sevens

Unlucky Sevens is an iMessage-first implementation of standard Catan for 3-4 players.

## Current Status

- the engine is audited, deterministic, and covered by package tests plus deterministic evals
- the repo is in `phase UI hardening`
- the next major step is `phase 10`

## Project Structure

Conceptual source-of-truth layout:

```text
.github/workflows/        # repo CI
App/                      # minimal iOS host app for the Messages extension
MessagesExtension/        # iMessage UI and transcript glue
Packages/
  ULS_CoreGame/           # pure Catan rules engine
  ULS_Transport/          # protocol, envelope, hashing, and payload handling
docs/                     # owner docs, PRD copies, audits, completed ExecPlans
scripts/                  # generate / clean helpers
Project.swift             # Tuist project manifest
Workspace.swift           # Tuist workspace manifest
Tuist.swift               # Tuist config entrypoint
```

`UnluckySevens.xcodeproj` and `UnluckySevens.xcworkspace` are generated by `./scripts/gen.sh`, may exist locally, and must remain untracked. Tuist manifests are the source of truth.

## Quick Start

```bash
bash ./scripts/gen.sh
open UnluckySevens.xcworkspace
```

Run the `MessagesExtension` scheme, open Messages in Simulator, and launch Unlucky Sevens from the app drawer.

## Test and Eval Commands

```bash
bash ./scripts/gen.sh
swift test --package-path Packages/ULS_CoreGame --skip ULS_CoreGameEvals
swift test --package-path Packages/ULS_CoreGame --filter ULS_CoreGameEvals
swift test --package-path Packages/ULS_Transport
xcodebuild -workspace UnluckySevens.xcworkspace -scheme MessagesExtension -destination 'generic/platform=iOS Simulator' build
```

For simulator smoke steps and manual QA, use [docs/quality/qa.md](docs/quality/qa.md).

## Architecture

- `ULS_CoreGame` owns rules, validation, determinism, and viewer-safe query helpers.
- `ULS_Transport` owns the message/protocol boundary.
- `MessagesExtension` owns iMessage orchestration and UI.

For the conceptual system design, use [docs/architecture/overview.md](docs/architecture/overview.md). For `STATE` / `INTENT`, authority, sessions, and validation, use [docs/architecture/messages-protocol.md](docs/architecture/messages-protocol.md).

## Docs Map

Start at [docs/index.md](docs/index.md).

Key owner docs:

- [docs/decisions.md](docs/decisions.md): locked product and architecture decisions
- [docs/architecture/overview.md](docs/architecture/overview.md): system architecture and code ownership
- [docs/architecture/messages-protocol.md](docs/architecture/messages-protocol.md): `STATE` / `INTENT`, sessions, validation, and payload rules
- [docs/product/mvp-contract.md](docs/product/mvp-contract.md): MVP promise, scope, and non-goals
- [docs/product/ui-flows.md](docs/product/ui-flows.md): player-facing flow summaries
- [docs/quality/qa.md](docs/quality/qa.md): practical gate, evals, and manual QA
- [docs/quality/golden-principles.md](docs/quality/golden-principles.md): highest-signal engineering rules
- [docs/plans/completed](docs/plans/completed): historical phase backfills

## Contribution Rules

- use Tuist manifests as the project source of truth
- do not commit generated Xcode files
- keep tests and evals green before pushing
