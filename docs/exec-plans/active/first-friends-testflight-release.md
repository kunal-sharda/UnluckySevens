# First Friends TestFlight Release

## Purpose and Outcome

Ship the first externally installable Unlucky Sevens beta to a small invited group through TestFlight. Success means the final UI branch is legacy-free and reproducibly verified, the signed Release build passes the repository gate and real-device multiplayer matrix, Apple processes the exact archived build without unresolved errors, invited friends can install it, and a two-account installed-TestFlight smoke proves the Messages extension still works outside the development install path.

## Execution Settings

- Validation profile: `release-critical`
- Delivery posture: `checkpointed`
- Rationale: The technical direction is settled, but App Store Connect upload, beta review submission, group assignment, and invitations are external distribution actions. Implementation and validation may proceed directly; the user is the authority for the final upload-and-invite checkpoint.

## Context and Boundaries

- This plan depends on the completed [Final UI Cohesion, Wiring, and Legacy Removal](../completed/final-ui-cohesion-and-legacy-removal.md), whose standard completion gate passed on 2026-08-17. That plan owns deletion of dead compiled UI, the repeatable screenshot harness, fresh iPhone/iPad evidence, and final UI/accessibility approval.
- [QA](../../quality/qa.md) selects the release lane; [device runbooks](../../quality/device-runbooks.md) own the hardware journeys; [constraint verification](../../quality/constraint-verification.md) owns completion language.
- [Architecture](../../../ARCHITECTURE.md), [decisions](../../decisions.md), and the product specs own engine, protocol, secrecy, compatibility, and player-facing behavior. This plan does not introduce gameplay, protocol, analytics, monetization, localization, or App Store public-release scope.
- Pre-TestFlight development transcripts are disposable. The first processed TestFlight build establishes the supported compatibility boundary; later protocol-breaking work must explicitly preserve or migrate states created by that build.
- The baseline hardware matrix is one iPhone and one iPad on separate Apple Accounts in one real Messages conversation. The multiplayer gate also requires enough participants/devices to exercise three- and four-player lobby ordering honestly; simulators may supplement but cannot replace the two-account hardware path.
- Apple currently requires an uploaded build to finish processing before testing. External friends require an external TestFlight group, an internal group first, beta test information, and TestFlight App Review for the first external build. Verify the live App Store Connect prompts at execution time: [TestFlight overview](https://developer.apple.com/help/app-store-connect/test-a-beta-version/testflight-overview), [external testers](https://developer.apple.com/help/app-store-connect/test-a-beta-version/invite-external-testers), [build upload](https://developer.apple.com/help/app-store-connect/manage-builds/upload-builds/), and [export compliance](https://developer.apple.com/help/app-store-connect/test-a-beta-version/provide-export-compliance-information-for-beta-builds).
- Secrets, Apple credentials, signing certificates, provisioning profiles, tester email addresses, and local device identifiers remain outside Git and tracked docs.
- Before any tester received a build, the owner chose to replace the uploaded Build 7 record with a durable developer-owned identity. The replacement candidate is version `1.0` build `8`, app `me.ksharda.games.unluckysevens`, extension `me.ksharda.games.unluckysevens.messagesextension`, and public web namespace `https://ksharda.me/unluckysevens`. The old App Store Connect record may be deleted only after the replacement identifiers sign successfully and immediately before creating the replacement record. Deletion permanently retires the old SKU and bundle identifier and requires an action-time owner confirmation.

## Milestones / Plan of Work

1. Close the UI dependency: delete all remaining dead compiled legacy presentation code, add its source guard, harden the single-DerivedData screenshot runner, inspect the complete iPhone catalog and representative iPad set, and pass the standard UI completion gate.
2. Freeze the beta candidate: record commit, bundle identifier, marketing version, unique build number, Xcode/SDK, minimum OS, signing team, and exact Release configuration; confirm no DEBUG UX Lab or retired presentation source ships.
3. Complete App Store Connect readiness without uploading: confirm agreements/access, app record and bundle ID, beta description, features-to-test copy, feedback and beta-review contacts, privacy answers and privacy-policy URL, export-compliance disposition, and any current Apple-required metadata. Review notes must explain that Unlucky Sevens is an iMessage-only app found in the Messages drawer, requires a conversation to exercise, and has no ordinary standalone home-screen experience.
4. Build and install the signed Release candidate on the real-device matrix. Run the shell, lobby, Messages lifecycle, turn-taking, gameplay-cohesion, accessibility, secrecy, and host-resize checks selected by the device runbook.
5. Run the multiplayer release matrix: three- and four-player lobby ordering, ordered seven/discard, mixed decline/counter Trade responses, the near-simultaneous acceptance race, stale-bubble recovery, bubble-bound resignation/draw/host-end lifecycle, session restart, read-only Player Record accuracy, and one complete standard match through victory.
6. Run `make release-gate PLAN=docs/exec-plans/active/first-friends-testflight-release.md`, create and validate the signed archive, and record the exact archive/build identity plus all warnings. Any unresolved validation, signing, privacy, or packaging error blocks distribution.
7. Present the frozen proof summary and exact upload candidate at the approval gate. Do not upload, submit for beta review, create/modify tester groups, or invite anyone before approval.
8. After approval, upload the validated archive, wait for Apple processing, resolve export-compliance prompts, create/confirm the internal group, create the external friends group, attach the build and test information, submit the first external build for TestFlight App Review, and invite only the user-approved friends after approval.
9. On the Apple-processed TestFlight build, perform an installed smoke on both Apple Accounts: install/update, find the app in the Messages drawer, invite/join/start, exchange canonical state across devices, reopen a stale bubble, recover through Games, complete a turn containing Trade or robber interaction, and inspect crashes/sessions/feedback before calling the friends beta live.

## Recovery and Rollback

- If local proof fails, do not archive or upload until the evidenced defect is corrected and affected contract rows rerun.
- If Apple processing or TestFlight App Review fails, retain the rejection/error record, correct only the evidenced packaging, metadata, compliance, or product defect, increment the build number when Apple requires a new upload, and present the replacement candidate again.
- If the installed TestFlight smoke or early friend use finds a release-blocking state fork, data leak, crash loop, unusable Messages entry path, or stranded-game defect, stop testing that build in App Store Connect, notify only the approved tester group, preserve diagnostic evidence, and ship no compatible-state-breaking replacement without an explicit migration decision.
- There is no remote kill switch. Stopping a TestFlight build prevents new testing access but does not substitute for communicating with testers who already installed it.

## Approval Gate

- Authority: User.
- Minimum honest proof: UI dependency passed; release gate passed; signed Release build installed on the hardware matrix; multiplayer matrix and full match passed; archive validation passed; App Store Connect metadata/privacy/export answers drafted; exact commit, version, build number, archive identity, external group name, and intended tester list presented.
- Iteration budget: One frozen upload candidate. If local validation or Apple processing/review fails, correct only the evidenced defect, rerun every affected contract row, and present a replacement candidate.
- Stop condition: Any failed or blocked contract row, unresolved archive/upload warning, unavailable required hardware/account, uncertain privacy/export answer, or missing user approval stops distribution. Never infer permission to invite testers.
- Authorized after approval: Upload the exact candidate, complete App Store Connect processing and first external beta review, add the approved build to the approved group, invite the approved testers, and run the installed-TestFlight smoke.

## Verification Contract and Fresh Review

<!-- verification-contract:start -->
| ID | Class | Source | Acceptance | Verification | Evidence | Status | Rationale |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TFR-001 | mechanical | user request and UI dependency | The final UI plan has every constraint and required review at pass, its completion gate passes, and no dead compiled legacy renderer/component remains | inspect dependency contract and commit; deleted-symbol source guard; dependency completion gate | commit:`27334681ca1c61ebabaea3fda4b24277b3221e7f`; report:`docs/exec-plans/completed/final-ui-cohesion-and-legacy-removal.md` | pass | The completed dependency has UCR-001–009 and all required reviews at pass, its standard gate passed, and the harness deleted-symbol guard is green |
| TFR-002 | mechanical | AGENTS.md and QA | The frozen candidate is reproducible from one commit and records bundle ID, version, unique build number, Xcode/SDK, minimum OS, signing team, configuration, and archive identity without tracked secrets | repository/status audit; Release build settings capture; archive metadata inspection | commit:`673813f`; artifact:`output/release/UnluckySevens-1.0-8-673813f.xcarchive`; report:archive-build-1.0.8-identity | pass | The frozen Build 8 archive records app `me.ksharda.games.unluckysevens`, extension `me.ksharda.games.unluckysevens.messagesextension`, version `1.0` build `8`, iOS 17.0 minimum, Release, and team `8B83G5AZ22`; app and extension executable SHA-256 values are `abe94d04…182` and `f9d29203…a8b` |
| TFR-003 | mechanical | QA release-critical profile | Core tests/evals, Transport tests, generic device build, Messages simulator build, workspace tests, harness audit, diff hygiene, and doc freshness pass through the release gate | `make release-gate PLAN=docs/exec-plans/active/first-friends-testflight-release.md` | command:fresh-practical-gate-pass-2026-08-24-139-core-10-evals-7-transport-362-messages; command:harness-audit-pass; command:harness-check-tests-45-of-45-pass; report:full-contract-still-open-for-post-upload-rows | pending | The final candidate’s exhaustive mechanical lane passes. The contract-aware completion gate remains correctly open while App Store processing, metadata, and installed-TestFlight rows are pending |
| TFR-004 | observable | device runbook real-device lane | The signed Release candidate installs on iPhone and iPad, appears in the Messages drawer, renders the approved shell without DEBUG surfaces, preserves accessibility/secrecy, and survives compact/expanded host resizing | two-device shell, lifecycle, gameplay-cohesion, accessibility, and secrecy checks with inspected evidence | report:Build-7-device-proof-historical; report:Build-8-clean-install-pass; report:owner-Build-8-focused-hardware-smoke-pass | pending | The exact frozen Build 8 archive clean-installed on the paired iPhone and iPad as version `1.0` build `8` under the replacement bundle identifier, with the legacy bundle absent afterward. The owner confirmed drawer discovery, lobby/invite exchange, privacy-link opening, Player Record interaction, and resize behavior on both devices. The broader release-runbook accessibility, secrecy, and lifecycle observations remain open |
| TFR-005 | observable | phase-13 manual TestFlight gate | Three- and four-player invite, join, rename, reopen, ordering, and start flows converge on the same canonical lobby/game state | real-device multiplayer lobby matrix with transcript/state evidence | report:pending | pending | Friends beta must not begin with broken membership or ordering |
| TFR-006 | observable | device runbook and phase-13 risk list | Ordered seven/discard rejects stale out-of-order publication; mixed Trade responses converge; two acceptances within about one second do not fork or brick the game | real-device discard sequence and Trade race probes with final canonical state/hash comparison | report:pending | pending | These are the highest-risk concurrent player actions |
| TFR-007 | observable | device runbook full standard-match pass and owner risk decision, 2026-08-24 | The paired-device candidate is exercised through the standard gameplay flow. For this first friends beta, the physically unobserved final victory transition is covered by deterministic full-match/victory automation and remains an explicitly accepted installed-TestFlight observation rather than a pre-upload blocker | owner paired-device report; deterministic full-match and victory-state suites; installed-TestFlight victory follow-up | report:owner-paired-gameplay-pass-except-victory-screen; command:10-of-10-deterministic-full-match-evals-pass; command:TurnVictoryV1Tests-5-of-5-pass; approval:owner-accepted-physical-victory-deferral-for-first-friends-beta | pass | The owner reported every intended paired-device checkpoint except the final victory screen, then explicitly chose to upload. Automation reaches victory across representative three- and four-player matches; physical terminal presentation remains a declared beta risk |
| TFR-008 | observable | recovery and terminal owner flows | Stale-bubble reopen, bubble-bound resign-as-active/waiting, draw reject/approve, host end including resigned host, New Game, and session restart behave consistently on both accounts; no lifecycle action is offered from a saved local snapshot | host-stability regression checklist with canonical-state evidence | report:pending | pending | Recovery and lifecycle failures can strand a beta game; lifecycle publication must remain attached to a real transcript session |
| TFR-009 | mechanical | Apple build-upload requirements | The signed archive validates; the uploaded build matches the frozen identity, completes Apple processing, and has no unresolved upload error, compliance block, or warning accepted without recorded disposition | archive validation; App Store Connect build metadata/status inspection | artifact:`output/release/UnluckySevens-1.0-8-673813f.xcarchive`; report:Build-8-upload-validated-ready-to-submit; report:macOS-and-visionOS-launch-prohibited-warnings-accepted-for-imessage-only-app | pass | Apple processed Build `1.0 (8)` on replacement record `6804632580` to Ready to Submit with binary state Validated. Live metadata confirms the replacement bundle, arm64/iOS 17/iPhone+iPad requirements, included iMessage app, hidden standalone icon, symbols, production entitlements, beta reporting, and no non-exempt encryption. The unchanged launch-prohibited warnings are accepted for this iOS-only iMessage product |
| TFR-010 | judgment | Apple TestFlight, privacy requirements, and QA release contract | Beta description, features to test, feedback and review contacts, iMessage-only reviewer instructions, privacy answers/policy URL, an accessible in-app privacy-policy link, export-compliance answers, group, and tester list are accurate and user-approved | user approval of the frozen distribution summary against live App Store Connect prompts; source inspection of the in-app policy link | report:developer-program-license-agreement-accepted; source:`MessagesExtension/Sources/Features/Utilities/AppSettingsView.swift`; command:AppWebLinks-and-transport-focused-11-of-11-pass; url:`https://unlucky-sevens-privacy.ksharda13.chatgpt.site`; report:public-policy-http-200; report:ksharda-me-DNS-unresolved; approval:pending | pending | A short policy reflecting the locked no-data-collection contract is publicly hosted for temporary App Store Connect use. Live TestFlight metadata/contact fields, privacy answers, and groups remain pending. Build 8's in-app Link still targets the intended long-term `ksharda.me` URL, which remains DNS-unresolved and must be routed or accepted as a beta-review risk before the full contract can pass |
| TFR-011 | observable | user first-friends beta outcome | Approved friends can install the Apple-processed build; the two-account installed-TestFlight smoke passes invite/join/start, canonical exchange, recovery, and one Trade or robber turn without development tooling | installed TestFlight smoke with build number and inspected evidence | report:pending | pending | Upload success alone does not prove the distributed extension works |
| TFR-012 | mechanical | compatibility and doc-freshness rules | Owner docs record the first supported TestFlight version/build and compatibility boundary; release notes and remaining beta risks are current without tracked credentials/tester data | semantic owner-doc audit; `make doc-freshness` | command:pending | pending | The first beta becomes a durable compatibility boundary |
| TFR-013 | observable | user real-device finding, 2026-08-21 compact-entry refinement, and Apple Messages selection contract | Opening Unlucky Sevens from the app drawer presents one fresh-launch compact entrance where the canonical robber lands on a raised wood-and-gold **Open Lobby** button; Skip settles the animation, Reduce Motion or Skip Animations starts settled, Open Lobby reveals the fresh invite lobby and requests expansion, while gameplay resumes only from a selected game bubble | route-policy/unit tests; focused cold-launch XCUITest with settled compact and resulting-lobby captures; installed-host drawer/bubble journey on iPhone and representative iPad check | command:focused-compact-launch-tests-11-of-11-pass; artifact:output/ui-screenshot-harness/20260821T081558Z-iphone; report:signed-release-build-2-installed-both-devices; report:physical-proof-pending | pending | The refined route and inspected simulator evidence pass; the saved-record continuation path is retired because it cannot recreate Apple-selected session continuity |
| TFR-014 | observable | user real-device finding and Apple iOS 17 Messages resize contract | Across lobby, gameplay, utilities, overlays/tutorial, and terminal screens, visible controls remain bounded and local board/scroll gestures work while the retained resize shield restricts host dragging to the intended top strip | source recognizer audit; owner-installed-host control, board, scroll, and resize samples on iPhone and representative iPad | report:resize-shield-retained-and-source-reviewed; report:physical-proof-pending | pending | The user retained the shield intentionally; physical proof must confirm its boundary without breaking local input |
| TFR-015 | observable | user two-device finding and Apple Messages lifecycle contract | A game bubble never renders the full tabletop inside iPad transcript presentation: transcript mode shows a compact game summary and explicit Open Game action, which requests the normal expanded game surface; while expanded, newer same-game messages refresh through `didReceive` without hijacking another open game | focused transcript-bridge model tests; source audit against Apple symbol docs; owner iPad transcript-card and paired live-update journey | command:MessagesTranscriptBridgeModelTests-pass; report:physical-proof-pending | pending | The compact bridge is implemented without a fixed-delay expansion policy; Apple controls whether and when a request changes presentation. Physical iPad bridge/expansion and paired live-update proof remain required |
| TFR-016 | observable | locked shared-tabletop geometry and user placement finding | Setup settlement/road placement, ordinary turns, and Build placement use the same board/ocean viewport size, aspect, center, rails, and mounted board identity for one settled host snapshot | pure board-budget tests; shared tabletop layout assertions; paired original-resolution setup/ordinary/build frames | command:GameShellLayoutMetricsTests-pass; artifact:pending | pending | Setup now uses the same action-surface reservation as ordinary/build play; original-resolution host evidence remains pending owner permission for the UI harness |
| TFR-017 | observable | user Your Games continuity finding and Apple `MSSession` contract | Saved ledger state is presented only as a read-only Player Record. It cannot open, reconnect, resend, resign, draw, host-end, or otherwise publish a game. Gameplay resumes only from a selected transcript bubble, while current-game lifecycle actions require that exact game to have a bound Messages session | pure Player Record/model and session-binding tests; source audit proving no saved-record action path; two-device bubble-resume and lifecycle journey | command:PlayerRecord-and-session-focused-tests-30-of-30-pass; command:MessagesExtensionTests-340-of-340-pass; report:physical-proof-pending | pending | Source and mechanical proof pass; builds 2 and 3 remain the evidence that arbitrary game-ID restoration cannot recreate Apple-selected session continuity, and physical build 4 proof is pending |
| TFR-018 | observable | user physical iPad finding and board-input contract | On iPad and iPhone, the first tap on a legal setup/build target selects it and the second tap on the same target commits exactly once; SpriteKit tap, pan, and pinch input and controls outside the board remain conventionally hittable | source recognizer audit; focused board-input tests where deterministic; owner iPad setup settlement/road sample plus iPhone regression sample | report:physical-proof-pending | pending | The experimental controller-wide SpriteKit exclusion was not merged because its physical result was inconsistent. Board geometry, target mapping, rules, and two-step commit semantics remain unchanged; physical proof is required before another recognizer change |
| TFR-019 | observable | user Your Games live-update finding and Apple `didReceive(_:conversation:)` contract | The disproven saved-record live-game path is absent. Selecting a real game bubble establishes the active game/session; newer valid same-game messages then advance it immediately, stale/equal revisions cannot roll it back, and another game cannot hijack it | focused state-observation/lifecycle tests; source audit proving no record-to-game navigation; paired physical-device bubble-bound waiting-player journey | command:TranscriptStateSelectionTests-pass; command:TranscriptDidReceiveContractTests-pass; command:SelectionWatchLifecycleTests-pass; command:MessagesExtensionTests-340-of-340-pass; report:signed-release-build-2-physical-fail; report:signed-release-build-3-physical-fail; report:build-4-physical-proof-pending | pending | The two failures are retained as evidence that local game-ID restoration is not a live Messages selection. Mechanical replacement proof passes; acceptance now tests only the platform-supported bubble-bound path on build 4 |
| TFR-020 | observable | user-approved Player Record replacement and tabletop stats refinement, 2026-08-23–24 | Player Record shows a device-local Catan record, honest game-derived honors, and games involving the current conversation's exact participant set, deduplicated by game ID. Stats use the locally recorded player identity, preserve finished outcomes/resignations, and never imply a stable iMessage thread identifier. Device-local storage remains an implementation and product-spec fact but adds no player-facing storage-scope label. The visual hierarchy reuses the shipped felt, paper, mini-terrain, road, Knight-card, dice, robber, and player-color vocabulary without emoji or generic achievement badges | table-driven model and ledger-migration tests; explicit in-progress accessibility-label regression; accessibility/source audit; focused iPhone and representative iPad Player Record captures | command:PlayerRecordModelTests-8-of-8-pass; command:TranscriptGameLedgerTests-14-of-14-pass; command:impeccable-detector-clean; artifact:output/ui-screenshot-harness/20260824T071036Z-iphone; approval:no-visible-device-scope-label | pending | The owner explicitly removed the redundant visible storage disclosure. Source inspection confirms the custom row label already used the full in-progress outcome text, including bubble-only continuation; the focused regression pins that semantic contract. Representative physical proof remains pending |
| TFR-021 | observable | user physical iPhone tutorial finding, 2026-08-23 | On the first setup tutorial screen, the setup-order card retains its accepted position and the board-placement card sits beneath it instead of covering the title/player-order region. The cards do not overlap, all copy remains visible, and production board/header geometry is unchanged | focused tutorial XCUITest ordering assertion and inspected iPhone capture; owner physical iPhone recheck | command:Release-build-and-UI-test-compilation-pass; artifact:focused-capture-blocked-before-tutorial; report:physical-proof-pending | pending | The correction is carried by installed Build 6; two bounded simulator attempts stopped at unrelated Messages host entry, so the owner physical iPhone recheck is authoritative next proof |
| TFR-022 | observable | user physical Build 5 and Build 6 findings, 2026-08-23, and Apple Messages activation/presentation/session contracts | Open Lobby consumes exactly once per fresh extension activation even if Messages repeats an activation callback during presentation transition; a later genuine activation gets a fresh entrance. Send Invite creates one provisional new-game publication, commits its `MSSession`, active context, ledger observation, preferred name, sent status, and dismissal only after Messages reports success, and on failure removes only that attempt's provisional binding while preserving a retryable lobby with an actionable error. A stale completion cannot mutate a newer attempt or a newly active conversation. Existing-game updates remain restricted to a matching selected or committed cached session. On Player Record, scrolling stays local and drags below the top grabber cannot collapse or dismiss the Messages surface, while the top strip retains host resizing | pure compact-launch activation tests; focused invite transaction and host-context tests for success, failure rollback, retry, stale completion, double tap, and conversation changes; existing-game session-policy regression tests; source-level ancestor/descendant gesture-boundary tests; full Messages extension tests; regenerated Release compile; owner physical Open Lobby, normal Send Invite, injected failure/retry where feasible, Player Record scroll, below-strip drag, and top-strip drag checks | command:focused-invite-lifecycle-session-error-tests-22-of-22-pass; command:MessagesExtensionTests-358-of-358-pass; command:regenerated-Release-simulator-app-extension-build-pass; report:fresh-TFR-022-constraint-audit-pass; report:fresh-invite-publication-behavior-audit-pass; report:signed-release-build-7-final-invite-correction-installed-both-devices; report:physical-proof-pending | pending | Source, mechanical, fresh-review, and paired installation proof pass. Invite state commits only after matching success; failure restores retry; repeated activation does not cancel a valid send; and conversation changes, explicit selection, true fresh activation, or resignation invalidate pending attempts so late callbacks cannot cross chats. Paired-device behavior observation remains pending |
| TFR-023 | judgment | user Player Record visual refinement, 2026-08-23–24 | Player Record centers the Table Honors and exact-player-group headings, uses consistent section spacing, replaces the generic table-history filler with direct empty-state copy, removes the redundant bottom device footer and `This device` header label, shows all five mini resource stamps with the unique leader in canonical paid build/development-card spending at full color and the others faded, omits redundant VP copy from Win rows, and avoids a large floating paper card for an empty group ledger. Populated ledger behavior, accessibility identifiers, read-only behavior, and canonical record inputs remain unchanged | table-driven resource-leader/model tests including ties and ignored other actors; source/accessibility audit; focused iPhone Player Record capture using production components; Impeccable detector; owner visual approval | command:PlayerRecordModelTests-8-of-8-pass; command:final-focused-iPhone-Player-Record-capture-pass; artifact:output/ui-screenshot-harness/20260824T071036Z-iphone; command:impeccable-detector-clean; approval:owner-visual-pass; command:current-signed-Release-paired-device-install-pass | pass | The implementation uses only audit-recorded canonical paid costs, shows no leader on a tie or absent spending, and preserves device-local semantics without adding protocol fields. The owner approved the final iPhone confirmation capture, and the same current Release artifact is installed in place on the paired iPhone and iPad. |
<!-- verification-contract:end -->

<!-- fresh-review:start -->
| Reviewer | Required | Verdict | Evidence |
| --- | --- | --- | --- |
| constraint-auditor | yes | pass | report:fresh-TFR-022-transaction-and-lifecycle-audit-2026-08-24 |
| behavioral | yes | pass | report:fresh-invite-publication-transaction-and-lifecycle-audit-2026-08-24 |
| product-ux | yes | pass | approval:no-visible-device-scope-label-2026-08-24 |
| accessibility | yes | pass | report:source-confirms-existing-bubble-guidance; command:PlayerRecordModelTests-8-of-8-pass |
| architecture | no | not-applicable | No architecture or protocol change is planned; route only if release evidence forces such a change |
<!-- fresh-review:end -->

## Living Record

### Progress

- [ ] Build 4 Player Record source implemented and mechanically verified; freeze the commit and signed Release identity only after focused UI and paired physical proof.
- [ ] App Store Connect, privacy, export, signing, and build identity ready.
- [ ] Signed Release hardware and multiplayer matrices passed.
- [ ] Release gate and archive validation passed.
- [ ] User approved the exact external distribution candidate and tester group.
- [ ] Apple processing/review and installed-TestFlight smoke passed.

### Decisions

- 2026-08-24: The owner chose a pre-distribution identity reset rather than ship the placeholder `com.unluckysevens.app` namespace. Build 8 uses `me.ksharda.games.unluckysevens` plus its nested Messages-extension identifier, publishes new messages under `https://ksharda.me/unluckysevens/msg`, and exposes `https://ksharda.me/unluckysevens/privacy` from Settings. URL decoding stays host-agnostic so prior development bubbles remain readable, while the new bundle identity intentionally starts a clean local app container. The locked proof set is canonical generation; focused web-link and transport compatibility tests; the full Messages suite with UI tests skipped; release gate; signed identifier/archive inspection; fresh constraint audit; and a clean iPhone/iPad install. The historical Build 7 evidence remains unchanged and is superseded only as the external candidate.
- 2026-08-24: Build 8 identity implementation passes canonical generation, 11/11 focused owned-domain and transport-compatibility tests, doc freshness, the exhaustive practical gate (139 Core, 10 full-match evals, 7 Transport, generic-device/simulator builds, and 362 Messages tests), and a signed Release generic-device build. Bundle inspection confirms app `me.ksharda.games.unluckysevens` and extension `me.ksharda.games.unluckysevens.messagesextension`, both `1.0 (8)`, signed for team `8B83G5AZ22`; the extension executable SHA-256 is `ec28df7a1383effa4911b4e1bc83b0ee0468ead3ff95588974b6cb5498dedc92`. The contract-aware release gate remains correctly blocked by pending external/device rows. `ksharda.me` does not currently resolve from the validation environment, so public policy hosting remains a hard external-submission blocker even though the binary contains the intended accessible link.
- 2026-08-24: After the owner’s action-time confirmation, App Store Connect record `6802221324` was removed before any tester received it; its Build 7, bundle `com.unluckysevens.app`, and SKU `unluckysevens-ios-1` are historical and cannot be reused. Apple Developer now has explicit replacement App IDs for `me.ksharda.games.unluckysevens` and `me.ksharda.games.unluckysevens.messagesextension`. The name was immediately reclaimed by replacement App Store Connect record `6804632580` with SKU `unluckysevens-ios-20260824`, iOS version `1.0`, English (U.S.), and full team access. Build 8 archive/upload remains next.
- 2026-08-24: Frozen commit `673813f` archived successfully as `output/release/UnluckySevens-1.0-8-673813f.xcarchive`; strict nested signature and identifier/version inspection pass. The exact archive uploaded successfully to replacement record `6804632580` and Apple reports that the package is processing. App and extension executable SHA-256 values are `abe94d04277062dca52c745a96f03fdeda0a6ea9c45e0613c1ade932415bb182` and `f9d29203a7877125c54880f723416fa2852d7eba93c4db20cf98e94256cf5a8b`. The unchanged macOS/visionOS launch-prohibited warnings remain accepted for the intentionally iOS-only iMessage product.
- 2026-08-24: Apple completed processing Build `1.0 (8)` to **Ready to Submit** with binary state **Validated**. Live metadata confirms bundle `me.ksharda.games.unluckysevens`, SDK/platform `23F81a`, symbols, no non-exempt encryption, included iMessage app, hidden standalone icon, minimum iOS 17.0, arm64, iPhone+iPad, and distribution entitlements with `get-task-allow=false`, `beta-reports-active=true`, and the exact app/extension identifiers. TFR-009 passes; public privacy hosting, metadata/groups, and clean Build 8 device proof remain separate blockers.

- 2026-08-23: Build 5 physical review found that the lobby entrance no longer progresses and Player Record can be dragged closed below the top grabber. Apple defines `didBecomeActive` as extension activation and `requestPresentationStyle` as a presentation-transition request; repeated activation handling must therefore be idempotent until a genuine resign/new activation boundary. The retained resize shield must prevent only Apple-owned ancestor pans below the grabber while remaining simultaneous with descendant ScrollView/board/control gestures. Build 5 is rejected, and the bounded Build 6 correction owns these two regressions without changing game/session state.
- 2026-08-23: Build 6 is frozen at `c2b44c0` and installed in place on the paired iPhone and iPad from one signed Release artifact. App executable SHA-256: `259c9bb7f87ec7205b76f85a9b41f47bad589aa277730048ce144ecfa50782e0`; extension executable SHA-256: `60eb0cbac2901ef7a28a8f85bf77c6bf6c815247afe157e5a6feedbc40571421`. Both bundles report version `1.0` build `6`; Release-string inspection found no UX Lab/debug markers. The focused lifecycle/gesture tests pass 5/5 and the full Messages extension suite passes 346/346. Device-local records and transcript bubbles were not erased.
- 2026-08-23: Build 6 physical testing rejected Send Invite: its button action fires, but publication never reaches Messages. The `a4d1d42` Player Record refactor made every unbound `.state(gameId:)` session fail, accidentally deleting the prior allowance for a genuinely new game's first `MSSession`. This is a demonstrated TFR-022 production defect, not validation-driven scope expansion. Build 7 will add an explicit new-state session policy that creates and caches a session only for a new invitation; matching selected/cached binding remains mandatory for all existing-game updates. Locked proof is the focused session-policy regression suite, the full Messages extension suite, canonical regeneration, regenerated Release compile, and paired-device Send Invite recheck.
- 2026-08-23: Build 7 source now routes only `inviteNewGame()` through `.newState(gameId:)`, creates and caches its fresh `MSSession`, and leaves every subsequent `.state(gameId:)` publication restricted to a matching selected or cached session. The focused regression suite passes 6/6, all Messages extension tests pass 348/348, canonical regeneration passes, and the regenerated Release simulator app/extension build passes with both bundles reporting `1.0 (7)`. Signed installation and paired physical Send Invite proof remain pending.
- 2026-08-24: The owner approved the final Player Record resource-stamp and spacing capture. A temporary signed Debug install exposed the expected DEBUG-only UX Lab on physical iPad and was immediately superseded in place by one signed Release artifact containing Build 7 plus the approved Player Record refinements. The repository device pipeline installed that Release artifact successfully on Kunal’s iPhone 16 Pro and the paired iPad (8th generation); unchanged device database identifiers confirm local app data was preserved. Physical Open Lobby, Send Invite, Player Record gesture, and paired gameplay observations remain the next checkpoint.
- 2026-08-24: The exhaustive practical gate passed 139 Core tests, 10 deterministic full-match evals, 7 Transport tests, generic-device and Messages simulator builds, and 350 workspace tests. Harness audit, all 45 harness tests, diff hygiene, and doc freshness also passed. The contract-aware release gate then correctly stopped at pending TFR-002 rather than rerunning the mechanical lane. Fresh behavioral, product/UX, and accessibility review found three release blockers: failed new-invite publication does not roll back its active/cached session state; Player Record no longer visibly discloses device-local scope; and its in-progress row’s custom VoiceOver label omits the bubble-only continuation instruction. Archive and TestFlight upload are stopped pending correction and affected proof.
- 2026-08-24: The owner removed visible device-local disclosure from Player Record as redundant presentation copy while retaining device-local storage semantics. Source reinspection corrected the accessibility finding: the custom in-progress row label already consumes `outcomeText`, whose canonical text says to open the game bubble. That label is now an explicit tested model property. The only remaining fresh-review production defect is invite-send rollback; physical release evidence and archive rows remain pending independently.
- 2026-08-24: The bounded invite correction will make new-game publication transactional. Each attempt gets an identity and provisional session; success alone commits the session, active context, ledger observation, preferred name, sent status, and dismissal. Failure removes only that attempt's provisional binding, leaves the prepared lobby and Send Invite retry available, and presents an actionable error. Stale callbacks and double taps cannot mutate a newer attempt. Proof covers success, failure rollback, retry with a fresh session, stale callback rejection, double-tap suppression, unchanged existing-game session policy, the full extension suite, regenerated Release compile, and paired-device publication/retry observation. No protocol, canonical game state, or gameplay rule changes are authorized.
- 2026-08-24: The transactional invite correction is implemented. `.newState` message construction no longer writes into the committed session cache. A dedicated attempt coordinator suppresses overlapping sends, accepts only the matching completion once, permits a fresh retry after failure, and ignores stale completions. `inviteNewGame()` owns the provisional session and defers session caching, ledger recording, preferred-name persistence, active-context selection, sent status, and dismissal until success; failure keeps the prepared lobby and maps to actionable retry copy. The focused invite/session/error suites pass 14/14, all 354 Messages extension tests pass with UI tests skipped, canonical regeneration passes, and the Release simulator app/extension build passes. Fresh audits and paired physical proof remain pending; no protocol or canonical-state shape changed.
- 2026-08-24: The initial behavioral audit found that attempt identity alone did not bind a pending invite callback to its originating Messages conversation. The correction now invalidates an in-flight attempt when the conversation object changes, on explicit message selection, on a genuine fresh activation, or when the extension resigns; a repeated callback within the same activation intentionally leaves the send valid. Final focused lifecycle/session/error proof passes 22/22, the full Messages suite passes 358/358, regenerated Release compilation passes, and fresh constraint and behavioral re-audits pass. The corrected artifact still requires paired-device publication/retry observation; no protocol, canonical state, or gameplay rule changed.
- 2026-08-24: The final corrected signed Release artifact was generated through the canonical device pipeline and installed in place, without uninstalling, on the connected iPhone and iPad. Both bundles report `1.0 (7)`. App executable SHA-256: `abb4e1fac9b4cdf5ae6f1be7e3e90df5524c27e8547cf6ed4bfc7b46ddd5503e`; extension executable SHA-256: `2cb3a6a3ddc674879d58856e42963bc4837c45d21ea7d9aaf27f6b3ddf6c0a52`. Installation succeeded on device IDs ending `801C` and `402E`; physical Send Invite and retry observation remain pending.
- 2026-08-24: Candidate source is frozen at `bd18703c1ed50ac1aadcc709d721fce168602941`. The fresh exhaustive practical gate passes 139 Core tests, 10 deterministic full-match evals through victory, 7 Transport tests, generic-device and Messages simulator builds, and all 358 Messages tests. The owner reported that paired-device gameplay checks passed except the final victory screen and explicitly accepted that physical terminal presentation as a first-friends beta observation; deterministic full-match and five focused victory-state tests remain its pre-upload proof.
- 2026-08-24: Archive `UnluckySevens-1.0-7-bd18703.xcarchive` succeeded and uploaded through Xcode. Apple processed build `1.0 (7)` to **Ready to Submit** and reports binary state **Validated**, no non-exempt encryption, included symbols, `get-task-allow=false`, `beta-reports-active=true`, iOS 17.0 minimum, iPhone+iPad device family, and an included iMessage app with hidden standalone icon. Archive app SHA-256: `819275194f79b0d63be2f687768b848301c165d5334a74670f40fcb68a95bbc6`; archive extension SHA-256: `34aaafd09b1d04014dce900487775b3a1057878127d42d0453796d8e41bd61a9`. The macOS and visionOS `LSApplicationLaunchProhibited` warnings are accepted as inapplicable to this iOS-only iMessage product. Account-holder acceptance of Apple’s updated Developer Program License Agreement and the TestFlight contact/privacy/review fields remain external blockers.
- 2026-08-24: The Account Holder accepted Apple’s updated Developer Program License Agreement and the warning cleared in App Store Connect. Live TestFlight inspection confirms that the beta description, feedback email, privacy-policy URL, review contact, phone, email, and review notes are empty. Repository source inspection also confirms no accessible in-app privacy-policy link, which `docs/quality/qa.md` defines as a release blocker. Build 7 remains valid for an internal installed-TestFlight smoke; external-friends submission requires approved metadata plus a public policy URL and a replacement build exposing that link.
- 2026-08-23: Build 4 physical iPhone review exposed the first setup tutorial board-placement card over the title/player-order region. The owner retained the setup-order card and requested only the visually first card move lower. The bounded correction stacks the board-placement card beneath the already-resolved setup-order card for that exact two-callout composition, leaves the second card and production tabletop geometry unchanged, and advances the local candidate to build 5 for unambiguous physical evidence.
- 2026-08-23: Build 5 is frozen at `35baa8a` and installed in place on the paired iPhone and iPad from one signed Release artifact. App executable SHA-256: `52b4c7d2d693cac55e1870086638dca97645a8b33001b5954ab5764016edd504`; extension executable SHA-256: `15845f009724956216a05b7fdc9100128ad4ea0cd101e6b5df25f460004157c9`. Both bundles report version `1.0` build `5`, and Release-string inspection found no UX Lab/debug markers. Device-local records and transcript bubbles were not erased.

- 2026-08-20: Fresh app-drawer entry now intentionally remains compact behind a one-launch entrance instead of attempting automatic expansion. The canonical masked-seven robber places onto a rectangular **Open Game** tile; Skip only settles the animation, Open Game reveals the already-prepared fresh invite lobby and requests expansion, and Reduce Motion or persisted Skip Animations starts settled. Selected bubbles, transcript presentation, and Your Games keep their existing game-specific routes. This replaces the retired automatic-expansion experiments for drawer entry without reviving a fixed-delay lifecycle workaround.
- 2026-08-20: Compact-entry validation attempt one proved Open Game reached the lobby but the common opener checked for product content before dismissing persisted DEBUG UX Lab chrome; attempt two corrected that order and then exposed a stale `uls.lobby.inviteTitle` selector. The current accessibility tree intentionally groups the visible `Invite Friends to Table` title under `uls.lobby.tableSurface`, so the focused route now asserts the exact visible label. Both are harness/assertion corrections evidenced by retained screenshots and trees, not product-scope expansion.
- 2026-08-20: Fresh slice audit required the compact entrance to consume the persisted Skip Animations preference through the shared `GameMotionPolicy`, isolate its activation token as pure one-shot state, and capture the invitation lobby before navigating to Your Games. Focused tests pass 11/11, and catalog attempt five at `output/ui-screenshot-harness/20260821T035847Z-iphone` contains separately labeled compact, invitation-lobby, and saved-games images. TFR-013 remains pending its required physical iPhone/iPad observations.
- 2026-08-21: The owner refined the fresh-entry CTA from a pale Open Game tile to a raised wood-and-gold **Open Lobby** button with a pressed state and lobby-specific people icon. The canonical robber retains its one-shot placement motion but now lands visibly on the button’s top edge. Transcript continuation keeps its separate **Open Game** wording. New compact visual proof is required before reusing the prior screenshot evidence.
- 2026-08-21: The refined Open Lobby route passed the clean installed iPhone simulator catalog at `output/ui-screenshot-harness/20260821T081558Z-iphone`; its distinct compact, invitation-lobby, and saved-games captures passed and the compact frame was inspected. That frame exposed one inset highlight resembling a second Messages drag handle, so it was removed without changing geometry or behavior. The single confirmation retry stopped before app entry because Messages would not open its apps drawer; the validation circuit breaker stopped further simulator retries. The final signed Debug build compiled and installed successfully on Kunal’s iPhone 16 Pro; the iPad was untouched. Owner physical visual confirmation is now the next proof.
- 2026-08-21: Owner physical review accepted the landed Open Lobby composition and requested slightly more approach travel. The robber now begins 30 points higher while retaining the same final contact point, spring timing, Skip behavior, and motion-accessibility policy. This is a bounded motion-only refinement; final physical observation remains the next visual proof.
- 2026-08-21: The higher-approach refinement passed canonical generation, the 11 focused route/token/motion tests, the interface detector, doc freshness, and diff hygiene. Its signed Debug device build succeeded, but the iPhone became unavailable before CoreDevice transfer; the previously installed Debug build remains on-device, and the current higher-approach artifact still needs an unchanged-build install after the phone reconnects.
- 2026-08-21: Froze the current pre-archive candidate at `b0364cab194a7b8809b16daefb02c0b142b43c26`, version `1.0` build `2`, Release/automatic signing, team `8B83G5AZ22`, iOS 17.0 minimum, Xcode 26.6 (`17F113`), and iPhoneOS 26.5 SDK. The first signed attempt exposed literal build `1` values in generated Info plists; the evidenced packaging correction now expands both bundle versions from the target settings. The full practical gate passed 139 Core tests, 10 deterministic match evals, 7 Transport tests, generic device and simulator builds, and 336 extension tests. One corrected signed artifact was installed in place on the paired iPhone 16 Pro and original iPad; both device inventories report version `1.0` build `2`. Extension executable SHA-256: `2a20cbb44cee7676d185b27de015f504289d18b65fa285111ba3663c313948b2`. Physical Messages testing is the next checkpoint; no device data was erased.
- 2026-08-23: Froze the Player Record candidate source at `a4d1d42`, version `1.0` build `4`, Release/automatic development signing, team `8B83G5AZ22`, iOS 17.0 minimum, Xcode 26.6 (`17F113`), and iPhoneOS 26.5 SDK. Canonical generation and the generic physical-device build succeeded; the identical app installed in place on the paired iPhone and iPad. App executable SHA-256: `4300a1bb079f50da7024f949b594e59c2d7b442d2e6a946396a9aa0ff4cc897f`; extension executable SHA-256: `6de208149dd3fbdf626f8fee3ec6ac20484a79ce2ff55beac0d9e60674a3e3f1`. Release-string inspection found no UX Lab/debug markers. Installation did not erase device-local data; physical behavior proof remains pending.
- 2026-08-10: This is a friends-only external TestFlight beta, not a public App Store release.
- 2026-08-10: The plan is release-critical and checkpointed. Local implementation and proof may proceed, but upload, beta-review submission, group mutation, and invitations stop for explicit user approval.
- 2026-08-10: The first processed TestFlight build establishes the compatibility floor; dev-era transcripts remain disposable.
- 2026-08-10: Real devices are authoritative for Messages behavior. Simulator catalogs remain required UI regression evidence but cannot substitute for the hardware multiplayer matrix.
- 2026-08-17: The approved UI baseline is commit `27334681ca1c61ebabaea3fda4b24277b3221e7f`. It has since been superseded for device proof by candidate `b0364cab194a7b8809b16daefb02c0b142b43c26`: app `com.unluckysevens.app`, extension `com.unluckysevens.app.messagesextension`, version `1.0`, build `2`, iOS 17.0 minimum, Release/automatic signing with team `8B83G5AZ22`, Xcode 26.6 (`17F113`), and iPhoneOS 26.5 SDK. Final archive identity remains pending after hardware proof.

### Discoveries

- 2026-08-23: Build 5 tutorial proof attempt one stopped before the changed tutorial screen because XCUITest could not resolve `uls.lobby.tutorial`; the retained simulator still visibly shows the Tutorial row in the correctly loaded invitation lobby. The one authorized unchanged retry stopped even earlier when Open Lobby failed to dismiss the compact gate. Both are classified as Messages host/accessibility harness instability rather than product failures, and both compiled the changed source plus UI-test assertion successfully. The validation circuit breaker stops simulator retries; the focused tutorial capture remains blocked and the signed physical Build 5 recheck is next.

- 2026-08-21: A canonical signed Debug device build of the compact-launch working tree succeeded for generic iOS in `DerivedData/DevicePipeline`. The first transfer was blocked while the paired iPhone tunnel was unavailable; after reconnecting, the unchanged prepared app installed successfully on Kunal’s iPhone 16 Pro as `com.unluckysevens.app`. The available iPad was intentionally untouched. TFR-013 remains pending the owner’s physical drawer/bubble/Games and motion observations.
- 2026-08-10: Apple’s current external-testing flow requires an internal group before an external group and TestFlight App Review for the first external build; live App Store Connect requirements must be rechecked at execution time.
- 2026-08-17: Apple’s current documentation still requires an app record before upload, processing before the build appears, beta description/review information for external testing, an internal group before the first external group, first-build TestFlight App Review, and a privacy-policy URL for all apps. Missing-compliance builds require export-compliance answers before review.
- 2026-08-17: Unsandboxed CoreDevice discovery sees a paired, available iPhone 16 Pro and iPad (8th generation). The same signed Release candidate (`1.0`/`1`) installed successfully on both through `devicectl`. TFR-004 remains pending the manual Messages-drawer/shell/lifecycle checks, and TFR-005–008 remain pending the two-account multiplayer journeys; simulator proof cannot substitute.
- 2026-08-17: App Store Connect redirected the available browser to an unauthenticated login, and no connected Chrome browser is available. Live app-record, agreements, build-number uniqueness, metadata, privacy, and group readiness remain pending user sign-in.
- 2026-08-17: Local signing is configured for automatic signing and one valid Apple Development identity is installed. No Apple Distribution identity is currently visible; Xcode may provision one during archive, but archive/validation remains downstream of the hardware and approval gates.
- 2026-08-17: The signed Release device build succeeds with automatic development provisioning. Its compiled extension contains no UX Lab/debug strings. The packaged extension now includes `PrivacyInfo.xcprivacy` declaring no tracking or collected data and the app-only `UserDefaults` reason `CA92.1`; the containing app declares `ITSAppUsesNonExemptEncryption=false`. A rebuilt product proves both files are present and valid. Distribution signing remains unproved until archive validation.
- 2026-08-17: Real-device drawer entry exposed an activation-routing defect: when no transcript message was selected, `LobbyDriverViewModel` automatically restored the last local-ledger game. The locked correction is drawer → fresh invite, bubble selection → that game, and Your Games → the chosen saved game. A focused production correction is in flight under TFR-013; optional table naming remains a separate product/schema decision until its exact presentation is confirmed.
- 2026-08-17: The route correction removed implicit local-ledger activation, added an explicit drawer-vs-selected-message policy, and reset transcript selection metadata for New Game. The focused policy tests passed 2/2. The installed-host route initially failed only because the test treated the lobby’s decorative board as gameplay; after correcting the assertion to canonical `game=-`/`phase=-`, the cold-install route passed and proved both fresh entry and preserved Your Games records. The same rebuilt Release candidate was reinstalled successfully on the connected iPhone and iPad; physical confirmation remains pending.
- 2026-08-17: The separate recovery regression also passed after a fresh cold install, proving Your Games still restores the exact saved game revision/hash and remounts the tabletop. This closes the automated regression risk; only the real-device drawer/bubble/Games observation remains for TFR-013.
- 2026-08-17: Physical follow-up found that the corrected fresh drawer entry no longer expanded automatically. The controller still requested `.expanded`, but it did so before publishing the fresh route during `didBecomeActive`. The narrow correction publishes the route first, requests expansion, and repeats the request on the next main-loop turn after Messages has settled activation. The focused route now also asserts `style=expanded`; rebuilt physical installation and user confirmation remain required.
- 2026-08-17: The first post-correction focused run stalled in the test transport after a clean, hash-matched install and was interrupted; its direct reuse failed before reaching the extension because the Messages `+` drawer would not open. Neither run exercised the new expansion assertion. The Release product nevertheless builds successfully and the bounded immediate-plus-150-ms expansion request is installed on both physical devices. Treat expansion as pending physical user confirmation, not automated pass.
- 2026-08-17: Physical review also found erratic lobby hit behavior and whole-screen host collapse dragging. Source inspection proved the shared shield’s capture recognizer returned `false` from `canPrevent`, so the required ancestor-host-pan dependency could not be enforced, while a two-point omnidirectional threshold captured incidental movement. TFR-014 owns the shared-root correction: only deliberate vertical content drags are captured, ancestor host pans are preventable below the grabber, and descendant controls/scroll views remain local. Per owner instruction, no harness may run without asking first.
- 2026-08-17: `MessagesHostGestureBoundaryTests` passed 4/4 after canonical generation, covering the grabber boundary, tap jitter, horizontal child movement, and deliberate vertical content drag. The Messages extension and test bundle compile. No UI harness was run for TFR-014; installed-host validation remains pending owner permission.
- 2026-08-17: Physical verification of that first gesture/expansion build failed: below-grabber drags still resized the host and drawer entry stayed compact. The follow-up audit found the shield omitted pan recognizers installed on its own protected root and simultaneously allowed same-root gestures, while the expansion retry could still precede Messages' documented compact drawer transition. The corrected shield now protects same-root and ancestor host pans while leaving true descendants local; expansion is re-requested after `didTransition(to: .compact)` settles. Compile proof is green, but both behaviors remain pending a new owner-approved physical installation and no harness may run without asking first.
- 2026-08-17: A second physical iPhone check proved that the eight-point recognizer still lost the race to Messages and that re-requesting expansion after every compact transition created a collapse/re-expand loop. Commit history confirmed `143f128` intentionally introduced a controller-wide shield for all routes. The restored design now claims below-grabber touches immediately, keeps delivery non-cancelling and descendant gestures simultaneous, prevents only same-root/ancestor pans, and treats expansion as a one-time activation phase that becomes settled after the first expanded transition. Physical proof remains pending.
- 2026-08-18: The restored gesture policy tests pass 3/3, canonical generation and Release compilation pass, and the identical signed extension binary (`2ac33b8d…5e49`) is installed on the connected iPhone 16 Pro and original iPad 8th generation. Messages was not resident on either device after installation, so no stale extension process remained. Owner physical verification of lobby taps, below-grabber drags, top-grabber collapse, board gestures, and scrollable utility content remains the TFR-014 acceptance gate; no UI harness ran.
- 2026-08-18: Owner physical verification failed again: the immediate custom recognizer did not stop the host drag and degraded lobby button recognition. Source comparison found the gameplay board uses a standard `UIPanGestureRecognizer`, while the redesigned lobby introduced an always-on vertical `ScrollView` whose top-edge pull can hand movement to Messages. The follow-up removes the immediate recognizer, restores a standard non-cancelling pan owner, makes the lobby a fixed composition with scroll fallback only when required, and gives Invite an immediate pressed/sending state while the asynchronous Messages publish completes. This correction requires fresh physical proof before TFR-014 can pass.
- 2026-08-18: The follow-up Release extension (`4149a611…3c6f6`) was built after all gesture/lobby source edits and installed successfully through unsandboxed CoreDevice on the paired iPhone 16 Pro and original iPad 8th generation. Both app records resolve to the new installation paths; the prior iPhone Messages process exited before termination and Messages was not resident on the iPad. TFR-014 remains pending owner physical verification, and no UI harness ran.
- 2026-08-18: Two-device review found that an open game did not receive the next update, prior game bubbles did not fold as expected, and selecting a transcript bubble opened in a small surface on iPad only. Apple documents that `selectedMessage` remains the originally selected bubble, `didReceive` is the live-update callback for newer same-session state, and `didSelect`/`didReceive` are not delivered in transcript presentation. The recent one-time expansion policy handled compact but ignored transcript presentation and also guarded the direct selected-message expansion request on activation timing. TFR-015 corrects both entry paths while preserving one `MSSession` per game; fresh two-device proof remains required and no UI harness may run without explicit permission.
- 2026-08-18: The TFR-015 Release extension (`e8ff16bd…c3c08`) compiled successfully and installed on the paired iPhone 16 Pro and original iPad 8th generation. Messages was not resident on the iPhone; the resident iPad Messages process was terminated after installation. The next physical journey must confirm iPad bubble entry expands, the open same-game shell advances on the other device's update, and same-session bubbles replace/fold rather than accumulate. No UI harness ran.
- 2026-08-18: The owner-provided iPad image proves bubble entry can remain in Apple-owned transcript presentation even after an automatic expansion request. The product currently mounts the full tabletop there, producing the miniature floating game. The supported correction is a dedicated compact transcript bridge with an explicit Open Game action; `MSMessagesAppViewController` conforms to `MSMessagesAppTranscriptPresentation`, and Apple documents no opt-out.
- 2026-08-18: Placement-size triage found a source-level contract violation at `GameShellView`: setup alone skips `actionSurfaceHeight + tabletopSectionSpacing` when computing the board budget. Git blame traces the exception to `27729a4` (Finish UI cohesion and save screenshot harness), while DESIGN and UI flows require setup to keep the shared game-screen board size. TFR-016 removes the phase-dependent viewport budget rather than adding setup-specific scaling.
- 2026-08-18: Your Games triage found that `resumeRecoveredGame` restores only the ledger snapshot. It neither validates/binds the selected message's game session nor records that recovery is unbound; the next publish may therefore fall through to a fresh `MSSession`, breaking transcript folding even though canonical game state remains intact. TFR-017 separates canonical recovery from session binding, preserves only proven same-game selected/cached sessions, and makes any new recovery chain explicit.
- 2026-08-18: Implemented the bounded correction: transcript presentation now renders a compact status bridge with **Open Game** and a 260-point content-fit cap; setup now reserves the same action region as ordinary/build play; Your Games now prefers an exact selected same-game session, falls back to its cached session, and labels an unbound cold recovery **Reconnect to Chat** before creating a new chain. Focused model/layout/session tests passed 24/24. The broader 317-test run compiled all product code and passed 316 test methods except one pre-existing SpriteKit occlusion test reporting three coordinate assertions; the scoped changed-area suite is green. The final Release extension (`ef7fcd03…13660`) built and was installed on the connected iPhone 16 Pro and original iPad 8th generation; Messages was not resident on either afterward. Physical paired proof and any UI harness remain pending explicit owner permission.
- 2026-08-19: Physical follow-up found two additional regressions: iPad no longer reliably commits the second placement tap, and a game resumed through Your Games remains stale when the other player publishes while it is open. TFR-018 isolates board/host recognizer arbitration without changing board geometry or rules. TFR-019 preserves the established `didReceive` state resolver and instead proves or repairs the recovered-game observation/presentation/session boundary. A separate bounded investigation continues on iPad bubble → expanded presentation. No UI harness is authorized by this work; ask the owner before starting it.
- 2026-08-19: The placement regression was the stock controller-wide `UIPanGestureRecognizer` competing in normal UIKit prevention/arbitration with SpriteKit. The shield is again a movement-only custom recognizer that prevents only root/ancestor host pans and cannot prevent descendant board or control gestures. Bubble expansion now makes one synchronous request from `didBecomeActive`, never loops after requesting, and retains **Open Game** because iPad expansion is host-controlled. Your Games explicitly requests expanded presentation when opening a recovery record, and successful `didReceive` processing retains the existing active-context watch. Canonical generation, generic simulator test compilation, and 25 focused unit tests passed. Physical two-device proof remains pending; no UI harness ran.
- 2026-08-19: A signed Release candidate was built successfully for physical devices; its extension SHA-256 is `9320e63d…14abd`. Installation did not occur: CoreDeviceService repeatedly failed to initialize, and the fallback `xcdevice` discovery saw only the Mac rather than either paired iPhone or iPad. This is an Apple device-service/discovery blocker, not product validation. Reconnect/unlock the devices and retry the exact artifact before owner testing; no device was changed and no UI harness ran.
- 2026-08-19: After both devices were unlocked, unsandboxed CoreDevice discovery found the paired iPhone 16 Pro and original iPad 8th generation. The identical signed Release extension (`9320e63d…14abd`) installed successfully on both. Messages was not resident on either device after installation, so no stale extension process remained. TFR-015/017/018/019 remain pending the owner’s physical journeys; no UI harness ran.
- 2026-08-19: Owner physical testing rejected that build: iPad placement still lost the second tap, iPhone drawer entry stopped expanding, iPad bubble entry remained transcript-sized, and a Your Games game stayed stale until reopen. Apple’s contract explains the coupled update symptom: `didReceive` is not delivered in transcript presentation. The regression came from treating an early expansion request as a completed one-shot and from still enrolling SpriteKit touches in the controller shield. The narrow correction restores pre-route requests at both activation boundaries, removes transition-driven retries, and excludes board/control/scroll touches from the shield entirely. Focused tests pass 24/24; fresh physical installation and owner proof remain required. No UI harness ran.
- 2026-08-19: The corrected signed Release extension (`e9238497…50701`) built successfully and installed on the paired iPhone 16 Pro and original iPad 8th generation; the iPad’s first CoreDevice tunnel attempt timed out and the single retry succeeded. Messages was not resident on either device after installation. Physical owner proof is still required before TFR-014/015/018/019 can pass; no UI harness ran.
- 2026-08-19: At owner request, `com.unluckysevens.app` was fully uninstalled from both paired devices and the identical corrected Release extension (`e9238497…50701`) was clean-installed on each. This deliberately cleared device-local recovery/preferences while leaving Messages transcript history intact. Messages was not resident afterward, so the next launch cannot reuse a stale extension process. Physical proof remains pending; no UI harness ran.
- 2026-08-19: The owner immediately rejected the clean install because iPhone drawer entry still remained compact. Dual activation-bound requests were still too early and were discarded while Messages presented its compact host. The replacement policy prepares the route during activation, requests once only after the initial compact `didTransition`, and uses a 500-ms `viewDidAppear` fallback for already-settled compact or transcript controllers. Expanded observation settles the phase, so later user collapse cannot retrigger it. Focused generation/build and 25/25 lifecycle/gesture/state tests pass; fresh Release installation and physical proof remain pending. No UI harness ran.
- 2026-08-19: The activation-settled Release extension (`d86229d1…6c3a5e`) built successfully. The rejected app was fully uninstalled and this exact build was clean-installed on both the paired iPhone 16 Pro and original iPad 8th generation; Messages was not resident afterward. Device-local recovery/preferences were cleared again by the requested clean reinstall. Expansion and paired live-update acceptance remain pending owner observation; no UI harness ran.
- 2026-08-19: The owner rejected the activation-settled build because iPhone drawer entry still did not expand. Work is now deliberately serialized: `MessagesViewController.swift` has been restored byte-for-byte to commit `2733468`, the last committed controller lifecycle associated with working iPhone expansion. This temporarily removes later controller-level fresh-route, transcript-bridge action, and recovered-game live-update wiring; those remain in source models/views but are intentionally not reintroduced until the iPhone baseline is physically reconfirmed. Build and iPhone-only clean installation are next; iPad will not be modified in this checkpoint. No UI harness is authorized.
- 2026-08-19: The exact-controller baseline Release extension (`33b4e5c1…bb2581`) built successfully and was clean-installed on the paired iPhone 16 Pro only. The iPad remains on the prior experimental build and was intentionally untouched. Messages was not resident on the iPhone afterward. Owner confirmation of first-open expansion is the sole next gate; do not resume iPad/transcript, launch-route, live-update, or gesture work before that result. No UI harness ran.
- 2026-08-19: The controller-only rollback did not restore iPhone expansion, so the entire repository at approved UI baseline commit `2733468` was generated in an isolated detached worktree, signed without source edits, and clean-installed on the paired iPhone 16 Pro only. The exact historical extension SHA-256 is `b0676eed4250e04f8c376093bd97cf61e4cd093935758a4ff8a9550657a1408f`; Messages was not resident after installation. The main worktree and iPad were untouched. Owner first-open expansion is the only next observation: a pass establishes the code baseline for forward bisection, while a failure moves diagnosis to Messages/iOS host state and a minimal extension repro. No UI harness ran.
- 2026-08-19: Owner testing of the exact full-repository baseline isolated the behavior: selected game bubbles still expand, while fresh launch from the bottom app selector remains compact. The next checkpoint changes only that historical controller by adding one uncoupled 300-ms post-activation expansion request; bubble selection cancels the pending request and keeps its known-working immediate request. Proof is a clean iPhone-only install followed by owner testing from the bottom selector and a bubble. iPad, gameplay, recovery, resizing, and the main product source remain untouched; no UI harness is authorized.
- 2026-08-19: The isolated historical-baseline drawer checkpoint built and clean-installed successfully on the iPhone 16 Pro. Its extension SHA-256 is `3395210ea9b38d38e777729dd7dddeea846ffbc6a7851de20be53591a9a38e63`; Messages was not resident after installation. The only source delta from commit `2733468` is the cancellable 300-ms post-activation expansion request. Owner must now test bottom-selector entry first and then a game bubble; the iPad and main product source remain untouched, and no UI harness ran.
- 2026-08-19: Owner physical testing passed both isolated iPhone entry paths: the bottom app selector expands after the uncoupled 300-ms request and an existing game bubble retains immediate expansion. The proven timing fix was then ported to the current full product controller, together with the existing transcript bridge and recovered-game expansion callbacks. The diagnostic container was fully uninstalled, absence of the old Unlucky Sevens app registration was checked, and a clean current build containing exactly one Messages extension (`com.unluckysevens.app.messagesextension`) was installed on the iPhone. Current extension SHA-256: `69edaf80cc2190682591bab5db621f53aee28360ac2e4b4a57d3cbe99e1dd8c6`. The iPad remains untouched; no UI harness ran.
- 2026-08-19: Owner testing rejected that first current-product port: unlike the historical baseline with the same controller timing, bottom-selector entry again stayed compact. The remaining lifecycle-relevant delta was the current resize shield linking its failure dependency to pan recognizers installed on the extension's own root view. That self-root link can hold the host presentation transition behind local gesture arbitration. The shield now links only Apple-owned ancestor pans while retaining descendant SKView/control/scroll exclusions and same-root runtime prevention. A clean current full build (`50509e49c…12bd17`) was installed on the iPhone, and Messages was not resident afterward. Owner bottom-selector and bubble proof is pending; iPad remains untouched and no UI harness ran.
- 2026-08-19: Owner testing rejected the ancestor-only shield build too, disproving that shield dependency as the sole expansion cause. Timeline review showed drawer expansion first regressed with fresh-entry routing, before gesture work. The next bounded current-product checkpoint therefore isolates host activation from no-message routing: activation requests expansion without publishing lobby/game state; a selected bubble still resolves immediately; a fresh drawer route commits after the expanded transition or, if the host supplies no transition callback, 350 ms after the proven 300-ms request. Your Games, transcript bridge, gameplay, layout, and gesture behavior remain otherwise unchanged. Proof is a clean paired physical install and owner selector/bubble checks; no UI harness is authorized.
- 2026-08-19: The activation-isolated current product built successfully with extension SHA-256 `ef2bc6aedd47d772af7c84b1602f53c356911a39502358ca4eb5e825e9cf5655`. Both prior device installs were fully removed. The first paired CoreDevice install attempt timed out for both devices; the single unchanged-artifact retry then installed successfully on the iPhone 16 Pro and original iPad 8th generation. Messages was not resident on either afterward. Owner must now test selector and bubble entry on both devices, with particular attention to whether iPad transcript entry presents the compact Open Game bridge. No UI harness ran.
- 2026-08-19: Owner testing rejected activation isolation because iPhone selector entry still stayed compact. The next binary-isolation checkpoint disables only the new transcript bridge branch at the shared root while retaining current routing, layout, gameplay, recovery, and gesture code. This directly tests the owner's iPad-change hypothesis without deleting the bridge implementation. A clean paired install and selector/bubble checks are required; no UI harness is authorized.
- 2026-08-19: The transcript-bridge-disabled isolation build (`4b7c85d5…dc4480`) compiled and clean-installed successfully on both the iPhone 16 Pro and original iPad 8th generation. The prior apps were fully uninstalled first. Messages was confirmed non-resident on iPhone; the post-install iPad process query lost its CoreDevice tunnel after installation, so stale-process proof is unavailable there even though installation itself succeeded. Owner selector/bubble checks on both devices are the only next evidence. No UI harness ran.
- 2026-08-20: Owner testing rejected the transcript-bridge-disabled build on iPhone, ruling out the iPad transcript branch as the drawer-expansion cause. The next bounded workaround re-enables the transcript bridge and prevents the full current lobby/game tree from mounting while the host is compact. Compact presentation renders only a lightweight opening surface with a 44-point explicit Open fallback; controller requests remain unchanged, and the full product mounts only after expanded presentation is observed. This preserves all later product work while isolating host transition from current UI/layout/state pressure. Clean paired physical proof is required; no UI harness is authorized.
- 2026-08-20: The compact-gate build (`f363d610…e201cc`) compiled successfully. Unlucky Sevens was fully uninstalled from both devices before installation. The iPhone installed on the first attempt; the original iPad disconnected on its first install and succeeded on the single unchanged-artifact retry. Messages was confirmed non-resident on iPhone; the iPad post-install process query hit a developer-disk-image connection error, so the owner should explicitly close Messages there before testing. Selector/bubble behavior remains pending owner proof. No UI harness ran.
- 2026-08-20: Owner testing found that compact entry still did not auto-expand, but the explicit Open control expanded immediately. That proves the request API, callback wiring, and host capability are intact and narrows the regression to lifecycle timing; the button is diagnostic fallback, not approved final UX. The next iPhone-only checkpoint sends the automatic request 100 ms after the compact surface itself appears, using actual rendered-host readiness rather than activation callbacks. iPad work is paused by owner direction. A clean iPhone install and selector proof are required; no UI harness is authorized.
- 2026-08-20: The rendered-compact timing build (`6828844f…dfed11`) compiled successfully. The prior iPhone app was fully uninstalled before this artifact was installed, and Messages was confirmed non-resident afterward. The original iPad was intentionally untouched. Owner selector proof is the only next check; if automatic expansion passes, remove the diagnostic Open surface before finalization. No UI harness ran.
- 2026-08-20: Owner testing rejected the compact gate and reported a new inability to swipe down, so that workaround is retired. The full dirty post-baseline tree was preserved exactly as safety commit `7a0352f` on `codex/post-273-recovery-safety`. A clean reconstruction branch, `codex/working-expansion-base`, now contains only approved UI baseline `2733468`, the physically proven 300-ms selector-expansion correction (`1d8bfef`), and the committed TestFlight packaging cherry-pick (`f999003`). That reconstructed Release extension (`9c03f208…d5daa`) was built, the compact-gate app was fully uninstalled, and the reconstructed app was installed on iPhone only; Messages was non-resident afterward. This checkpoint restores historical gesture ownership and tests packaging independently. iPad is paused and no UI harness ran.
- 2026-08-20: Owner testing rejected the reconstructed source-plus-packaging build. Before attributing causality to packaging, the exact previously owner-approved artifact—not a rebuild—was reinstalled as the byte-for-byte control. The iPhone app was fully uninstalled, exact extension SHA-256 `3395210ea9b38d38e777729dd7dddeea846ffbc6a7851de20be53591a9a38e63` was installed from its retained product, and Messages was non-resident afterward. If this artifact again expands, packaging/build-output isolation continues; if it now fails, host/registration state invalidates the prior source comparison. iPad remains paused and no UI harness ran.
- 2026-08-20: At owner direction, the 300-ms patch artifact was fully uninstalled and replaced with the exact retained Release product from pure commit `2733468`, with no source delta, packaging commit, or later work. Installed extension SHA-256: `b0676eed4250e04f8c376093bd97cf61e4cd093935758a4ff8a9550657a1408f`. Messages was non-resident afterward. iPad remains untouched and no UI harness ran.
- 2026-08-20: At owner direction, a new isolated branch `codex/selector-expansion-200ms` was created from pure `2733468`; its only change is the cancellable post-activation expansion request delayed by 200 ms (`c6573e0`). The pure-base app was fully uninstalled from iPhone before installing the new Release extension (`d62dc76c…114125`), and Messages was non-resident afterward. No packaging or later product work is present, iPad remains untouched, and no UI harness ran.
- 2026-08-20: Historical review corrected the control boundary: `2733468` is not pre-iPad-layout source; it already contains `d73cbe2` (controller-owned settled host canvas and Messages transition integration) and `17eea96` (stable-host evidence contract). The true immediate pre-layout checkpoint is `b180957`. An exact unpatched Release build from `b180957` succeeded, the prior iPhone app was fully uninstalled, extension SHA-256 `9349557f…8a08f67` was installed on the iPhone, and Messages was confirmed non-resident afterward. This is the first valid source control for the owner's remembered selector-expansion behavior. iPad remains untouched and no UI harness ran; owner selector/bubble proof is pending.
- 2026-08-20: Owner initially observed bottom-selector automatic expansion on the exact unpatched `b180957` control, then corrected the result: repeated entry is inconsistent on the same installed bytes. Therefore this control does **not** isolate `d73cbe2` as a deterministic root cause. It establishes a host-lifecycle/timing-sensitive failure that can occur even before the responsive host-layout work; later controller/layout work may still change its probability. Stop blind fixed-delay patches. The next proof must record the physical callback/request/presentation-style sequence across repeated selector entries on one unchanged diagnostic build before changing production behavior. iPad remains paused and no UI harness ran.
- 2026-08-20: Handoff baseline restored to the latest full product checkpoint `7a0352f` (`codex/post-273-recovery-safety`), not a historical control or isolated delay branch. One signed Release artifact built successfully through canonical generation; extension SHA-256 is `9add2110…2416c1`. The old app was fully uninstalled from both devices and this identical artifact installed successfully on the paired iPhone and original iPad. Messages was confirmed non-resident on iPhone; the post-install iPad process check timed out after installation, so the owner should close Messages before its first check. No UI harness ran.
- 2026-08-20: Owner reports a repeatable iPad orientation distinction: bubble entry in portrait stays on the small surface, while landscape bubble entry presents the larger surface; the tray also presents the larger surface. Treat this as a likely Messages-owned presentation/available-space behavior, not a proven app defect. Apple documents that drawer entry begins compact, message selection normally begins expanded, transcript presentation receives a host-provided maximum size, and `requestPresentationStyle` is a request. The code does not currently override `contentSizeThatFits(_:)`. The next audit must record actual style, context, bounds, safe area, orientation, lifecycle callbacks, and request/transition results for portrait/landscape × tray/bubble before changing layout or adding another retry.

### 2026-08-20 New-Task Handoff

Canonical source is again the original `ui-ux-single-device-testing` branch, now fast-forwarded to clean integration commit `a0473d8`. That commit selectively carries durable routing, recovery, lobby-target, board-budget, transcript-summary, test, and documentation changes from the saved post-baseline work. Fixed-delay expansion code, the compact-opening timing gate, and the unverified custom gesture-boundary implementation/tests are excluded. Do not resume from `b180957`, `2733468`, or any retired `codex/*expansion*`/post-baseline diagnostic branch; they were investigation controls rather than release sources.

Implemented in the latest checkpoint:

- approved UI-cohesion/tutorial/gameplay surfaces and stable host-responsive tabletop from the completed UI plan;
- drawer entry routes to a fresh lobby, while a selected game bubble or explicit Your Games record routes to that game;
- transcript presentation has a compact game bridge and explicit **Open Game** request;
- recovered games preserve or explicitly reconnect the matching transcript session instead of borrowing a different game’s session;
- active recovered games retain same-game observation after successful receipt, with stale/equal and different-game guards;
- conventional lobby targets, a non-reentrant sending state, and scroll fallback only when the lobby content genuinely exceeds its host;
- shared setup/ordinary/build tabletop budget intended to keep board/ocean/rails the same size;
- TestFlight packaging metadata, privacy manifest, and export-compliance declaration.

Required audit order in the next task:

1. **Messages host matrix:** on the unchanged latest build, record ten repeated entries for iPhone selector/bubble and iPad portrait/landscape tray/bubble. Capture actual presentation style/context, controller identity, bounds, safe area, activation callbacks, expansion requests, and transition callbacks. Do not diagnose from visual size alone and do not add a fixed-delay patch.
2. **System gesture ownership:** reconcile the custom resize shield with Apple’s iOS 17 contract that Messages owns interactive vertical resizing and gesture conflict handling. Verify normal control/board gestures, collapse/expand, and whether the requested “top-only” resize boundary is technically enforceable without fighting the host; amend the locked product contract if the platform owns the gesture.
3. **iPad input:** physically verify setup settlement and road double-tap, Build placement, board pan/pinch, and ordinary controls in both orientations. Confirm the shield never receives SpriteKit/control/scroll touches.
4. **Routing and continuity:** physically verify fresh drawer lobby; bubble-only resume; bubble-bound waiting-player live update without reopen; read-only Player Record; bound current-game lifecycle actions; and no cross-game hijack. Test both expanded and transcript/portrait cases because Apple does not deliver all lifecycle behavior identically in transcript presentation.
5. **Responsive composition:** inspect portrait/landscape originals for stable board aspect/center, rail ordering, bottom clearance, and consistent setup/ordinary/build viewport. Treat host-provided transcript size as a separate compact product surface, not a scaled full game.
6. **Release proof:** after the above behavior is settled, run the owner-approved focused harness, then the release-critical gate, archive validation, two-account multiplayer matrix, and TestFlight installed smoke. TFR-002–019 remain pending unless already marked pass in the contract.

- 2026-08-20: The clean integration branch selectively retained the fresh-drawer/selected-game route, explicit recovery-session binding, same-game state-selection coverage, transcript summary bridge, lobby target/sending feedback, and shared setup/ordinary board budget. It excluded the 100/200/300/500-ms expansion experiments, compact-opening gate, custom gesture-boundary source/tests, and their overstated top-only product contract. Canonical generation and the generic Messages simulator build pass. The focused routing/recovery/transcript/state/layout suites pass, followed by the complete `MessagesExtensionTests` target. That full run exposed an unrelated existing SpriteKit test setup bug: it asserted camera-relative occlusion geometry before initializing the camera; the test now exercises the production camera path and the complete target passes. No UI harness ran.
- 2026-08-20: Clean integration commit `a0473d8` was fast-forwarded onto the original `ui-ux-single-device-testing` branch. Its signed Release extension SHA-256 is `af65514bf2355383897e3dd7b12e818203b8dece7ef2e5c754de1e2d6bb0f3b1`. Unlucky Sevens was fully uninstalled from the paired iPhone 16 Pro and original iPad (8th generation), then the identical app artifact was installed on both; both device app inventories report version `1.0` build `1`. Messages was confirmed non-resident on iPhone. The iPad disconnected during the optional process query after its successful install and app-record verification, so the owner should close Messages before testing there. No UI harness ran.

### Draft TestFlight Metadata

- Beta description: “Unlucky Sevens is an iMessage-first tabletop strategy game for friends. Invite players in a Messages conversation, build a shared island economy, trade resources, and race to 10 points.”
- What to Test: “Play from invite through victory. Please focus on joining and reopening games from their Messages bubbles, setup placement, rolling and building, player and Bank/Port trades, development cards, robber/discard flows, current-game resign/end actions, and Player Record accuracy. Report any state mismatch between players, missing Messages bubble, clipped screen, or action that cannot continue.”
- Review notes: “This is an iMessage-only app. It has no ordinary standalone home-screen experience. In Messages, open a conversation, tap `+`, choose Unlucky Sevens from the apps drawer, and open the extension. Send an invitation into the conversation, then use another participant/device to open the invitation, join, and continue. The game stores recovery state locally and publishes canonical game state through Messages.”
- App privacy draft: “Data Not Collected” by the developer. No accounts, analytics, ads, tracking, developer server, or third-party SDKs. Display names/preferences and read-only game records are stored locally; players deliberately send game state through Apple Messages. Final answers remain subject to the live App Store Connect questionnaire.
- Export compliance draft: no non-exempt encryption; the app implements no cryptography and relies only on Apple operating-system/Messages transport. `ITSAppUsesNonExemptEncryption=false` is packaged.
- Proposed internal group: `Internal Smoke`. Proposed external group: `First Friends`.
- Pending owner inputs/actions: feedback email; beta-review contact details/phone; and the eventual tester invitations or public-link choice. The tester list is not required before the first external TestFlight review submission.

## Validation and Outcome

- Automated: Historical lanes passed 350 and later 358 Messages tests as features landed. The current Build 8 exhaustive practical gate passes doc freshness, canonical generation, 139 Core tests, 10 deterministic full-match evals, 7 Transport tests, generic device and Messages simulator builds, and all 362 Messages tests with UI tests skipped. Harness audit, all 45 harness self-tests, diff hygiene, and the focused 11/11 owned-domain/legacy-URL compatibility suite pass. Broader release-runbook and installed-TestFlight behavior evidence remains pending.
- Simulator/UI: The compact fresh-launch behavior passes canonical generation, 11/11 focused route/token/motion tests, harness audit, doc freshness, diff hygiene, fresh slice review, and the earlier clean installed catalog. The Open Lobby refinement passed at `output/ui-screenshot-harness/20260821T081558Z-iphone`; inspected captures show the robber landed on the raised wood-and-gold button, the resulting fresh invitation lobby, preserved saved games, and exact recovery. A misleading inset highlight was then removed; the bounded confirmation retry failed in Messages drawer setup before product entry, so current final visual proof is deferred to the signed Debug build now installed on the physical iPhone.
- Real devices: Build 7 behavior evidence is historical after the identifier reset. On 2026-08-24, the legacy `com.unluckysevens.app` Build 7 installation was explicitly removed from the paired iPhone and iPad, clearing its device-local app records/preferences while leaving Messages transcript history intact. The exact frozen Build 8 archive then installed successfully on both; post-install inventories show only `me.ksharda.games.unluckysevens`, version `1.0` build `8`. The owner then passed the requested paired-device smoke: Messages drawer discovery, Open Lobby and invite exchange, privacy-policy Link opening, Player Record scrolling/drag containment, and intended resize-strip behavior. This is signed-archive evidence, not an installed-TestFlight pass.
- 2026-08-22: Owner testing failed TFR-019 on build 2 with the precise sequence selected game A → Your Games → saved game B in the same group chat → peer publishes newer B. Clicking a B bubble repaired the display, proving explicit selection worked while the recovered-game observation boundary did not. The bounded correction makes stale selected-message polling unable to replace an explicitly recovered game, restarts/retains the cancellable watch when Your Games changes the active game, and keeps newer same-game `didReceive` activation plus other-game storage guards. Build 2 remains rejected; source-frozen build 3 is now installed and the exact paired-device sequence must pass before TFR-019 can return to pass.
- 2026-08-22: The exact build 3 paired-device retest failed identically. This falsifies the stale-polling/cancellation explanation despite its valid defensive guards. The remaining evidence points to a platform/session-selection boundary: Your Games can restore canonical local state but cannot assign `MSConversation.selectedMessage`, reconstruct a terminated `MSSession` from an identifier, or enumerate transcript messages. Do not add another timer/watch patch; decide whether Your Games becomes explicitly recovery-only until the player selects/reconnects a bubble, or whether live cross-session recovery justifies a server/CloudKit authority expansion.
- 2026-08-23: User chose the simpler platform-honest product: replace Your Games with a read-only Player Record. Build 4 source removes saved-record Open/Reconnect/Resend/archive/lifecycle controls and the new-session recovery escape hatch; records show device-local overall results plus an exact-current-participant-set view. Resign, draw, and host end moved to current-game Game Information and require the exact game to have a bound session. The fresh constraint audit initially caught one transcript bridge fallback from last-active ledger state; that fallback and its Open Game affordance are now removed when no current game exists, and the focused regression passes. Canonical generation, generic Messages build, final iPhone 17 simulator test-bundle compilation, 30 focused tests, all 340 extension tests, the 18-test record/ledger follow-up, the 7-test bridge/record follow-up, six focused tabletop-record tests, the focused iPhone 17 Player Record UI capture, Impeccable static detector, and fresh constraint re-audit pass. Representative iPad capture, signing, install, and paired physical proof remain pending.
- Archive/App Store Connect: Build 7’s processed upload is historical. Frozen Build 8 commit `673813f` archived, passed local identifier/version/signature inspection, uploaded to replacement Apple ID `6804632580`, and completed Apple processing as **Validated / Ready to Submit**. A temporary public privacy policy now returns HTTP 200 at `https://unlucky-sevens-privacy.ksharda13.chatgpt.site`. Build 8's in-app Link still targets `https://ksharda.me/unluckysevens/privacy`, whose host remains DNS-unresolved; that does not prevent using the temporary URL as App Store Connect metadata, but it remains an explicit in-binary review risk.
- Distribution: Build `1.0 (8)` is Ready to Submit and has no groups or testers. No invitation or beta-review submission has occurred.

The release is planned but not yet verified or distributed.
